# LLM claim extraction (optional — falls back to heuristics without a key)
LLM_PROVIDER=openrouter
LLM_EXTRACTION_ENABLED=true
OPENROUTER_API_KEY=your-key-here
OPENROUTER_MODEL=google/gemini-2.5-flash
